# Tide Atlas To Do

- [x] Build a Hugo static site from the archived sea level rise experiments.
- [x] Add labeled axes to the Atlas view.
- [x] Restore the world map in the Atlas view.
- [x] Use meaningful anomaly and trend colors.
- [x] Add a visual legend for color meaning.
- [x] Add Pulse and Rings visualization modes.
- [x] Remove decorative background wave lines.
- [x] Credit the UH Sea Level Center fast-delivery data source.
- [x] Improve layout behavior across narrow, tablet, and desktop windows.
- [x] Update Braid view to color traces by annual anomaly and sort by active-year anomaly.
- [ ] Add vertical land movement data from SONEL.
  - Source: https://www.sonel.org/-Vertical-land-movements-.html?lang=en
  - Target current solution: ULR20 GNSS vertical velocities.
  - Use the vertical velocities table for station-level land motion rates and uncertainty.
  - Match SONEL GNSS stations to tide gauge stations by nearby coordinates and station metadata.
  - Add local relative sea level context: tide gauge trend, vertical land motion, and inferred ocean component.
  - Visualize subsidence/uplift separately from sea level anomaly so the map distinguishes water rise from land motion.
  - Add legend entries and source credit for SONEL vertical land movement data.
- [x] Add a p5.js 3D view of station movement.
  - Added a `3D` mode backed by p5.js/WebGL; each latitude/longitude station is projected onto a rotating globe and drawn as an outward/inward vector.
  - Added in-view controls for year, scale exaggeration, and combined/sea-only/land-proxy components.
  - The land-motion component is currently a clearly labeled proxy derived from tide-gauge trend until SONEL GNSS velocities are imported.
- [ ] Replace the p5 3D view's land-motion proxy with matched SONEL GNSS vertical velocities after the SONEL import is added.
- [ ] Add a data build step that fetches or imports external source files reproducibly.
  - [x] Add root npm scripts for rebuilding and validating atlas data.
  - [x] Document source acquisition for the archived UH input files and world land polygons.
    - Added `docs/source-acquisition.md` with the current archived UH-derived local input paths, world-atlas/Natural Earth land source, rebuild commands, validation commands, generated outputs, and the remaining provenance gap for original UH endpoint fetch/import automation.
  - [x] Make rebuild timestamps deterministic or split generated metadata from deterministic data for cleaner diffs.
- [x] Add lightweight data validation for station coordinates, year ranges, missing values, and trend units.
  - Added `scripts/validate-data.mjs` plus Node tests for coordinate bounds, chronological years, missing numeric values, station counts, and millimeter/mm-year unit expectations.
- [x] Add a short methodology note explaining anomaly baseline, trend calculation, and station filtering.
  - Added an on-page methodology note covering the annual anomaly baseline, ordinary least-squares trend calculation, and station filtering assumptions; export validation now checks that this note remains present.
- [x] Add URL state for selected mode, year, and station so views can be shared.
  - `static/js/app.js` now reads `?mode=...&year=...&station=...` on load and keeps those parameters synchronized with `history.replaceState` as the user changes mode, year, or station.
- [x] Add keyboard-accessible controls for mode switching, playback, and station navigation.
  - Mode tabs now expose ARIA tab state and support arrow/Home/End navigation; playback exposes pressed state and a Space shortcut hint; featured station buttons expose selected state and arrow/Home/End focus movement.
- [x] Add static export checks for the generated `public/` output.
  - Added `scripts/validate-export.mjs` and `npm run validate:export`; it rebuilds with Hugo and verifies required exported files, non-empty assets, local asset references, Atlas canvas markup, and the UH source credit.
- [x] Fix Hugo 0.123 language-code template compatibility so `hugo server` starts locally.

## Work log

### 2026-05-12T17:16:04-04:00 — p5.js 3D station movement mode
- Tasks attempted: advanced the p5.js 3D view TODO by adding a WebGL globe mode with station vectors and local controls.
- Root cause / pattern: the static atlas already had several canvas modes, but no p5.js entry point or DOM target; the remaining 3D task needed a separate p5 surface that could coexist with the existing atlas canvas and mode URL state.
- Files changed: `layouts/index.html`, `static/js/app.js`, `static/js/station-3d.js`, `static/vendor/p5.js`, `static/css/site.css`, `tests/validate-data.test.mjs`, `TODO.md`; `public/` was regenerated by Hugo export validation.
- Commands run:
  - `node --test tests/validate-data.test.mjs` (RED first: failed because `static/js/station-3d.js` and 3D mode markers did not exist).
  - `node --test tests/validate-data.test.mjs` (GREEN: all 8 tests passed after adding the p5 view and source invariants).
  - `npm test && npm run validate:data && npm run validate:export && pygount --format=summary --folders-to-skip=".git,node_modules,venv,.venv,__pycache__,.cache,dist,build,.next,.tox,.eggs,*.egg-info,public,leafletjs-implementation/Back End/node_modules" .` (all tests, atlas data validation, Hugo export validation, and codebase inspection passed; pygount reported 1,669 files and 42,714 code lines excluding public and dependency/build folders).
  - `date --iso-8601=seconds`, `git status --short`, `git diff --stat`, and a source/export search for the new 3D markers.
- Results: the page now loads local `static/vendor/p5.js` plus `static/js/station-3d.js`, exposes a `3D` mode in the tablist/URL state, switches the stage to a p5/WebGL globe in that mode, and provides year/scale/component controls for station displacement vectors.
- New TODOs added: replace the p5 land-motion proxy with matched SONEL GNSS vertical velocities after the SONEL import lands.
- Remaining work: SONEL vertical land movement import/matching and replacing the temporary 3D land-motion proxy with real GNSS velocities.
- Git status summary at end of work: modified `TODO.md`, `hugo.yaml`, `layouts/index.html`, `scripts/build-data.mjs`, `static/css/site.css`, `static/data/sea-level-atlas.json`, `static/js/app.js`; untracked `docs/`, `package.json`, `scripts/validate-data.mjs`, `scripts/validate-export.mjs`, `static/js/station-3d.js`, `static/vendor/`, `tests/`.
- Diff stat at end of work: tracked diff shows `TODO.md` 134 lines changed before this log update, `hugo.yaml` 2, `layouts/index.html` 30, `scripts/build-data.mjs` 4, `static/css/site.css` 81, `static/data/sea-level-atlas.json` 2, `static/js/app.js` 91; new p5/static/test files are listed in git status.

### 2026-05-12T16:12:27-04:00 — Source acquisition documentation
- Tasks attempted: completed documentation for archived UH input files and world land polygon source/rebuild steps.
- Root cause / pattern: rebuild scripts existed for atlas data and the world map, but the source provenance and exact reproducibility commands were not documented outside the scripts themselves.
- Files changed: `docs/source-acquisition.md`, `tests/validate-data.test.mjs`, `TODO.md`; `static/data/world-land.json` and `public/` were regenerated by validation commands.
- Commands run:
  - `node --test tests/validate-data.test.mjs` (RED first: failed because `docs/source-acquisition.md` did not exist).
  - `node --test tests/validate-data.test.mjs` (GREEN: all 7 tests passed after adding the documentation).
  - `npm test && npm run validate:data && npm run build:world-map && npm run validate:export && pygount --format=summary --folders-to-skip=".git,node_modules,venv,.venv,__pycache__,.cache,dist,build,.next,.tox,.eggs,*.egg-info,public,leafletjs-implementation/Back End/node_modules" .` (all tests, data validation, world-map rebuild, Hugo export validation, and codebase inspection passed; pygount reported 1,667 files and 42,558 code lines excluding public and dependency/build folders).
  - `date --iso-8601=seconds`, `git status --short`, and `git diff --stat`.
- Results: added `docs/source-acquisition.md` covering the canonical archived UH-derived local inputs, University of Hawaii Sea Level Center credit, `scripts/build-data.mjs`, `world-atlas@2` / Natural Earth land polygon source, `scripts/build-world-map.mjs`, rebuild/validation commands, expected generated outputs, and the remaining provenance gap for scripted original-UH endpoint acquisition.
- New TODOs added: none.
- Remaining work: SONEL vertical land movement, p5.js 3D view, and a future scripted fetch/import path if stable original UH source endpoints are identified.
- Git status summary at end of work: modified `TODO.md`, `hugo.yaml`, `layouts/index.html`, `scripts/build-data.mjs`, `static/css/site.css`, `static/data/sea-level-atlas.json`, `static/js/app.js`; untracked `docs/`, `package.json`, `scripts/validate-data.mjs`, `scripts/validate-export.mjs`, `tests/`.
- Diff stat at end of work: tracked diff shows `TODO.md` 134 lines changed after this log update, `hugo.yaml` 2, `layouts/index.html` 26, `scripts/build-data.mjs` 4, `static/css/site.css` 30, `static/data/sea-level-atlas.json` 2, `static/js/app.js` 87; new untracked documentation/test/script files are listed in git status.

### 2026-05-12T15:09:37-04:00 — Keyboard-accessible controls
- Tasks attempted: completed keyboard-accessible controls for mode switching, playback state, and featured station navigation.
- Root cause / pattern: the native buttons/select/range were keyboard reachable, but custom mode tabs lacked ARIA tab state and arrow-key behavior, the play/pause button did not expose pressed state, and featured station buttons did not expose list/selected state or roving focus shortcuts.
- Files changed: `layouts/index.html`, `static/js/app.js`, `tests/validate-data.test.mjs`, `TODO.md`; `public/` was regenerated by Hugo validation.
- Commands run:
  - `node --test tests/validate-data.test.mjs` (RED first: failed because mode tabs, playback pressed state, station-list role, and keyboard handlers were missing).
  - `node --test tests/validate-data.test.mjs` (GREEN: all 6 tests passed after implementation).
  - `npm test && npm run validate:data && npm run validate:export && pygount --format=summary --folders-to-skip=".git,node_modules,venv,.venv,__pycache__,.cache,dist,build,.next,.tox,.eggs,*.egg-info,public,leafletjs-implementation/Back End/node_modules" .` (all tests, atlas data validation, Hugo export validation, and codebase inspection passed; pygount reported 1,666 files and 42,554 code lines excluding public and dependency/build folders).
  - `date --iso-8601=seconds`, `git status --short`, and `git diff --stat`.
- Results: mode controls now use `role="tab"`/`aria-selected` with ArrowLeft/ArrowRight/Home/End switching; play/pause updates `aria-pressed`; featured gauges are exposed as a list and support ArrowUp/ArrowDown/Home/End focus movement with `aria-pressed` on the selected station.
- New TODOs added: none.
- Remaining work: SONEL vertical land movement, p5.js 3D view, and archived UH/world-land source acquisition documentation.
- Git status summary at end of work: modified `TODO.md`, `hugo.yaml`, `layouts/index.html`, `scripts/build-data.mjs`, `static/css/site.css`, `static/data/sea-level-atlas.json`, `static/js/app.js`; untracked `package.json`, `scripts/validate-data.mjs`, `scripts/validate-export.mjs`, `tests/`.
- Diff stat at end of work: `TODO.md` 100 lines changed before this log update, `hugo.yaml` 2, `layouts/index.html` 26, `scripts/build-data.mjs` 4, `static/css/site.css` 30, `static/data/sea-level-atlas.json` 2, `static/js/app.js` 87.

### 2026-05-12T14:03:36-04:00 — Methodology note and export guard
- Tasks attempted: completed the methodology-note TODO and made the static export validator guard against losing it.
- Root cause / pattern: the page footer mentioned the baseline briefly, but there was no durable on-page explanation of anomaly baseline, trend calculation, and station filtering; export validation also did not check for methodology content.
- Files changed: `layouts/index.html`, `static/css/site.css`, `scripts/validate-export.mjs`, `tests/validate-data.test.mjs`, `TODO.md`; `public/` was regenerated by Hugo validation.
- Commands run:
  - `node --test tests/validate-data.test.mjs` (RED first: failed with missing methodology-note phrases in `index.html`).
  - `npm test && npm run validate:data && npm run validate:export` (GREEN: all Node tests, atlas data validation, Hugo build, and static export validation passed).
  - `pygount --format=summary --folders-to-skip=".git,node_modules,venv,.venv,__pycache__,.cache,dist,build,.next,.tox,.eggs,*.egg-info,public,leafletjs-implementation/Back End/node_modules" .` (codebase inspection completed: 1,666 files, 42,505 code lines excluding public and dependency/build folders).
  - `date --iso-8601=seconds`, `git status --short`, and `git diff --stat`.
- Results: the home page now includes a styled methodology note explaining the first-five-valid-years baseline, ordinary least-squares trend calculation, and station filtering assumptions; export validation fails if the note disappears.
- New TODOs added: none.
- Remaining work: SONEL vertical land movement, p5.js 3D view, archived UH/world-land source acquisition documentation, and keyboard accessibility.
- Git status summary at end of work: modified `TODO.md`, `hugo.yaml`, `layouts/index.html`, `scripts/build-data.mjs`, `static/css/site.css`, `static/data/sea-level-atlas.json`, `static/js/app.js`; untracked `package.json`, `scripts/validate-data.mjs`, `scripts/validate-export.mjs`, `tests/`.

### 2026-05-12T12:58:40-04:00 — Static export validation
- Tasks attempted: added an automated check for generated `public/` output so the static Hugo export can be validated beyond “build completed.”
- Root cause / pattern: previous validation covered source atlas JSON and Hugo template compilation, but did not inspect the exported artifact for required files, empty assets, missing local references, or key page content.
- Files changed: `scripts/validate-export.mjs`, `package.json`, `tests/validate-data.test.mjs`, `TODO.md`; `public/` was regenerated by Hugo validation.
- Commands run:
  - `node --test tests/validate-data.test.mjs` (RED first: failed because `scripts/validate-export.mjs` did not exist; after initial implementation, failed on minified unquoted `id=atlasCanvas`, which identified the validator needed to handle Hugo minified HTML).
  - `npm test && npm run validate:data && npm run validate:export` (GREEN: all Node tests, atlas data validation, Hugo build, and static export validation passed).
  - `pygount --format=summary --folders-to-skip=".git,node_modules,venv,.venv,__pycache__,.cache,dist,build,.next,.tox,.eggs,*.egg-info,public,leafletjs-implementation/Back End/node_modules" .` (codebase inspection completed: 1,666 files, 42,473 code lines excluding public and dependency/build folders).
  - `git diff --stat` and `git status --short`.
- Results: `npm run validate:export` now rebuilds `public/` with Hugo and checks required export files, non-empty assets, HTML local asset references, Atlas canvas markup, and UH Sea Level Center source credit.
- New TODOs added: none.
- Remaining work: SONEL vertical land movement, p5.js 3D view, source acquisition documentation, methodology note, and keyboard accessibility.
- Git status summary at end of work: modified `TODO.md`, `hugo.yaml`, `layouts/index.html`, `scripts/build-data.mjs`, `static/data/sea-level-atlas.json`, `static/js/app.js`; untracked `package.json`, `scripts/validate-data.mjs`, `scripts/validate-export.mjs`, `tests/`.

### 2026-05-12T11:55:15-04:00 — Shareable URL state
- Tasks attempted: made Atlas views shareable by persisting selected visualization mode, year, and station in the query string.
- Root cause / pattern: existing `static/js/app.js` only read `mode` at startup; year and station always reset to the latest year and default gauge, and UI changes did not update the URL.
- Files changed: `static/js/app.js`, `tests/validate-data.test.mjs`, `TODO.md`.
- Commands run:
  - `node --test tests/validate-data.test.mjs` (RED first: new URL-state invariant failed because `getInitialStateFromUrl`/`syncUrlState` did not exist).
  - `npm test && npm run validate:data && hugo --minify` (GREEN: all Node tests, atlas data validation, and Hugo static build passed).
  - `pygount --format=summary --folders-to-skip=".git,node_modules,venv,.venv,__pycache__,.cache,dist,build,.next,.tox,.eggs,*.egg-info,public,leafletjs-implementation/Back End/node_modules" .` (codebase inspection completed: 1,665 files, 42,412 code lines excluding public and dependency/build folders).
  - `git diff --stat` and `git status --short`.
- Results: loading `?mode=braid&year=2000&station=<id>` now initializes those controls when valid, and mode/year/station interactions update the current URL with `history.replaceState` for copying/sharing.
- New TODOs added: none.
- Remaining work: methodology note, keyboard accessibility, static export checks, source acquisition documentation, SONEL vertical land movement, and p5.js 3D view.
- Git status summary at end of work: modified `TODO.md`, `hugo.yaml`, `layouts/index.html`, `scripts/build-data.mjs`, `static/data/sea-level-atlas.json`, `static/js/app.js`; untracked `package.json`, `scripts/validate-data.mjs`, `tests/`.

### 2026-05-12T10:50:34-04:00 — Deterministic atlas rebuild metadata
- Tasks attempted: removed timestamp-only churn from rebuilt atlas data while preserving generated metadata.
- Root cause: `scripts/build-data.mjs` wrote `generatedAt: new Date().toISOString()`, so every `npm run build:data` changed `static/data/sea-level-atlas.json` even when source data was unchanged.
- Files changed: `scripts/build-data.mjs`, `tests/validate-data.test.mjs`, `static/data/sea-level-atlas.json`, `TODO.md`.
- Commands run:
  - `node --test tests/validate-data.test.mjs` (RED first: deterministic `generatedAt` test failed because the builder emitted wall-clock time).
  - `npm test && npm run validate:data && npm run build:data && npm run validate:data && hugo --minify` (GREEN: all Node tests, data validation, data rebuild, revalidation, and Hugo build passed).
  - `pygount --format=summary --folders-to-skip=".git,node_modules,venv,.venv,__pycache__,.cache,dist,build,.next,.tox,.eggs,*.egg-info,public,leafletjs-implementation/Back End/node_modules" .` (codebase inspection completed: 1,665 files, 42,383 code lines excluding public and dependency/build folders).
  - `git diff --stat` and `git status --short`.
- Results: `generatedAt` is now deterministically derived from the rebuilt dataset's `lastYear` as `<lastYear>-12-31T00:00:00.000Z`; current atlas output now uses `2018-12-31T00:00:00.000Z`, eliminating wall-clock rebuild diffs.
- New TODOs added: none.
- Remaining work: document archived UH/world-land source acquisition, methodology note, URL state, keyboard accessibility, static export checks, and SONEL/p5.js features.
- Git status summary at end of work: modified `TODO.md`, `hugo.yaml`, `layouts/index.html`, `scripts/build-data.mjs`, `static/data/sea-level-atlas.json`; untracked `package.json`, `scripts/validate-data.mjs`, `tests/`.

### 2026-05-12T10:28:34-04:00 — Hugo server compatibility fix
- Tasks attempted: fixed the local Hugo build/server failure reported from `layouts/index.html`.
- Root cause: the template referenced `.Site.Language.Locale`, but Hugo v0.123.7 exposes the configured language code through `.Site.LanguageCode`; `locale` in `hugo.yaml` was not the compatible Hugo config key.
- Files changed: `layouts/index.html`, `hugo.yaml`, `TODO.md`.
- Commands run:
  - `hugo --minify` (reproduced failure before fix; passed after fix).
  - `timeout 8s hugo server --port 1337 --bind 127.0.0.1` (server started successfully; command ended by timeout after confirming readiness).
  - `git status --short` and `git diff --stat`.
- Results: `hugo --minify` now builds 3 pages successfully, and `hugo server --port 1337` reaches "Web Server is available at //localhost:1337/".
- New TODOs added: none.
- Remaining work: continue existing validation/export, source acquisition, methodology, URL state, accessibility, SONEL, and p5.js 3D tasks.


### 2026-05-12T09:47:09-04:00 — Autonomous development pass
- Tasks attempted: advanced lightweight data validation and reproducible data-build entry points.
- Files changed: added `package.json`, `scripts/validate-data.mjs`, `tests/validate-data.test.mjs`; rebuilt `static/data/sea-level-atlas.json` (timestamp-only data change).
- Commands run:
  - `node --test tests/validate-data.test.mjs` (RED first: failed because `scripts/validate-data.mjs` did not exist; GREEN after implementation: passed 2 tests).
  - `node scripts/validate-data.mjs` (passed generated atlas validation).
  - `npm test && npm run validate:data && npm run build:data && npm run validate:data` (passed; rebuild wrote 266 stations and revalidated).
  - `pygount --format=summary --folders-to-skip=".git,node_modules,venv,.venv,__pycache__,.cache,dist,build,.next,.tox,.eggs,*.egg-info,public" .` (codebase inspection completed; repo dominated by JavaScript plus archived/unknown source files).
- Results: data validation is now available as `npm run validate:data`; root `npm test` runs the Node test suite; `npm run build:data` exercises the existing atlas data builder.
- New TODOs added: document source acquisition; make rebuild output deterministic or isolate `generatedAt` to avoid timestamp-only diffs.
- Remaining work: external source import/fetch workflow, methodology note, URL state, keyboard accessibility, static export checks, and SONEL/p5.js features.
- Git status summary at end of work: modified `TODO.md`, modified `static/data/sea-level-atlas.json`; untracked `package.json`, `scripts/validate-data.mjs`, `tests/`.
